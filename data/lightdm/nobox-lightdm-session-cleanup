#!/usr/bin/sh

# Match the setup boundary: XDM bookkeeping belongs only to X11 sessions.
if [ "${XDG_SESSION_TYPE:-}" = wayland ] || [ -z "${DISPLAY:-}" ]; then
    exit 0
fi

exec /usr/share/X11/xdm/Xreset
